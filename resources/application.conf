ktor {
     login = ${?LOGIN_ENV}
     password = ${?PASSWORD_ENV}

    deployment {
        port = 8080
        port = ${?PORT}
    }
    application {
        modules = [ br.com.dias.andre.ApplicationKt.module ]
    }
}
